# Messages for the errors declared in hivepaas_app/hperrors/errors_spec.go.
# Adding or removing an error there means editing this file.

ERR_SPEC_MOUNT_TARGET_DUPLICATED = "Two volume mounts use the same target path '{{.Target}}'"
ERR_SPEC_SETTING_TYPE_UNCLASSIFIED = "Setting type '{{.Type}}' has no spec block name, so it cannot be exported"
ERR_SPEC_PASSPHRASE_REQUIRED = "A passphrase is required to export with encrypted secrets"
